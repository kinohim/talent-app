# PostgreSQL接続文字列（Neon/Supabase等）。docs/design/basic-design.md 1.2参照
DATABASE_URL="postgresql://user:password@localhost:5432/talent_app?schema=public"

# Auth.js（NextAuth）用シークレット。`openssl rand -base64 32` 等で生成する
NEXTAUTH_SECRET="replace-with-random-secret"
NEXTAUTH_URL="http://localhost:3000"
